-- Book 02a - Box with Text-- Tested for copying into Codea-- This does not work as expected-- Use this function to perform your initial setupfunction setup()    x = 10    y = 20end-- This function gets called once every framefunction draw()        -- First attempt does not work as expected    -- comment out rect to see text        -- This sets a dark background color     background(0)    -- This sets the line thickness    strokeWidth(20)        -- This draws the rectangle    rect(x, y, WIDTH/2, HEIGHT/2)        -- Draw the text    font("Georgia")    fill(255)    fontSize(20)    textWrapWidth(70)    text("Hello World!", x+100, y+ 150)    end